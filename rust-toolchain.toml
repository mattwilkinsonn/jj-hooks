[toolchain]
# Exact pin so rust-overlay + CI share one derivation and caches survive.
# rust-overlay reads this file; bump the channel here on version changes.
channel = "1.96.0"
components = ["rustfmt", "clippy"]
